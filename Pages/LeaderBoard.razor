@page "/leaderboard"
@using HACKH.Models
@using System.Net.Http.Json
@using MudBlazor


<MudTable Items="@Elements" >
 @*  <ColGroup>
        <col style="width: 60px;" />
        <col />
        <col style="width: 60%;" />
        <col style="width: 60px;" />
        <col />
    </ColGroup>*@
    <HeaderContent>
        <MudTh>#</MudTh>
        <MudTh>Név</MudTh>
        <MudTh>Pontszám</MudTh>
        <MudTh>Értékelés</MudTh>
      
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="#">@context.Id</MudTd>
        <MudTd DataLabel="Név">@context.Name</MudTd>
        <MudTd DataLabel="Pontszám">@context.Points.ToString()</MudTd>
        <MudTd DataLabel="Értékelés">@context.Classification.ToString()</MudTd>
    </RowTemplate>

</MudTable>


@code {
    private List<Owner> Elements = new List<Owner>();

    protected override void OnInitialized()
    {
        Owner gela = new Owner(1, "Balasz Kivancs", 2,4);
        Owner gela2 = new Owner(1, "Giko Bulbul", 6, 2);
        Owner gela3 = new Owner(1, "Giko Kulbul", 6, 2);
        gela.Classification = Role.Aranyember;
        gela2.Classification = Role.Semleges;
        gela3.Classification = Role.Gazfickó;
        Elements.Add(gela);
        Elements.Add(gela2);
        Elements.Add(gela3);

    } 


}